package com.example.Musify.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // Files are now stored in MongoDB GridFS, no need for static resource handler
}

